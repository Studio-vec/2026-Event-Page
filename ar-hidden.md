# AR '세션장별 불꽃' 기능 숨김 기록 (2026-08-18)

사용자 요청으로 **AR 페이지 진입 경로를 전부 제거**했습니다.
파일 자체는 지우지 않았으므로, 아래 4곳만 되돌리면 그대로 복구됩니다.

## 현재 상태

- `ar.html`, `markers_all.mind`, `compile.html`, `image/marker-0*.png` — **저장소에 그대로 남아 있음** (수정 없음)
- `vercel.json`의 `.mind` 캐시 헤더 규칙 — 그대로 둠
- 다만 링크가 전부 사라져서 사용자는 AR 페이지에 도달할 수 없음
  (주소창에 `/ar.html`을 직접 입력하면 여전히 열림. 완전 차단이 필요하면 `vercel.json`에 리다이렉트 추가 필요)

## 제거한 4곳

| # | 파일 | 내용 |
|---|---|---|
| 1 | `index.html` | 메인의 `세션장별 불꽃 / AR 카메라 인식` 버튼. 남은 `나의 불꽃 유형` 버튼을 감싸던 `.cta-row` 2단 래퍼도 함께 제거해 전폭 버튼으로 변경 |
| 2 | `info.html` | 경품 탭 하단 `📷 AR 불꽃 확인` 보조 버튼 |
| 3 | `info.html` | 하단 내비게이션의 `📷 AR 불꽃` 링크 (3칸 → 2칸) |
| 4 | `info.html` | FAQ 2건 — `AR이 작동하지 않아요`, `마커 인식이 잘 안 돼요` |

`index.html`의 `.cta-row` CSS는 복구 편의를 위해 **삭제하지 않고 주석과 함께 남겨뒀습니다.**

## 복구 방법

가장 확실한 방법은 이 커밋 직전 버전에서 해당 블록을 가져오는 것입니다.

```bash
git log --oneline -- index.html info.html
git show <이 커밋의 부모>:index.html > /tmp/old-index.html
```

`ar.html` 자체는 건드리지 않았으므로 링크 4곳만 되살리면 즉시 동작합니다.

## 같이 확인할 것 (복구 시)

- `info.html` 응모 방법 탭의 `두 이벤트를 모두 완료하고…` 문구 — 현재는 AR 없이도 말이 되지만, 원래 AR + 유형테스트 2개를 가리키던 표현임
- 현장 인쇄물/QR이 `/ar.html`을 직접 가리키고 있는지
